% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/swadlr-list.R
\name{swadl_id_names}
\alias{swadl_id_names}
\title{List ID-name mappings for SWADL metadata}
\usage{
swadl_id_names(
  what = c("topics", "indicators", "measures", "dimensions", "geographies"),
  topic = NULL,
  indicator = NULL
)
}
\arguments{
\item{what}{The type of metadata to list. One of:
\describe{
\item{\code{"topics"}}{Broad categories that group related indicators}
\item{\code{"indicators"}}{Specific data series that can be retrieved with
\code{\link[=get_swadl]{get_swadl()}}}
\item{\code{"measures"}}{Ways of presenting indicator data (e.g., nominal vs
real wages)}
\item{\code{"dimensions"}}{Demographic categories for subsetting data (e.g.,
gender, race)}
\item{\code{"geographies"}}{Geographic units (national, regions, divisions,
states)}
}}

\item{topic}{For \code{what = "indicators"}, optionally filter to a specific
topic ID.}

\item{indicator}{For \code{what = "measures"} or \code{what = "dimensions"}, optionally
filter to those available for a specific indicator ID.}
}
\value{
A tibble. The columns depend on \code{what}:
\describe{
\item{topics}{\code{id}, \code{name}}
\item{indicators}{\code{id}, \code{name}, \code{topic_id}, \code{updated_date}}
\item{measures}{\code{id}, \code{name}, \code{format}}
\item{dimensions}{\code{dimension_id}, \code{dimension_name}, \code{value_id},
\code{value_name}}
\item{geographies}{\code{id}, \code{level}, \code{name}, \code{abbr}}
}
}
\description{
Returns a tibble of ID-name mappings for SWADL metadata. Use this to
understand what each ID represents.
}
\examples{
\donttest{
# List all topics
swadl_id_names("topics")

# List all indicators
swadl_id_names("indicators")

# List indicators for a specific topic
swadl_id_names("indicators", topic = "wages")

# List measures for a specific indicator
swadl_id_names("measures", indicator = "hourly_wage_percentiles")

# List dimensions
swadl_id_names("dimensions")

# List geographies
swadl_id_names("geographies")
}
}
\seealso{
\code{\link[=swadl_indicator]{swadl_indicator()}} for detailed information about a single
indicator, \code{\link[=get_swadl]{get_swadl()}} for fetching time series data.
}
