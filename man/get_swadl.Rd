% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/series.R
\name{get_swadl}
\alias{get_swadl}
\title{Get time series data from SWADL}
\usage{
get_swadl(
  indicator,
  measure,
  date_interval = c("year", "month"),
  geography = "national",
  dimension = "overall",
  date = NULL
)
}
\arguments{
\item{indicator}{Indicator ID (e.g., \code{"hourly_wage_percentiles"}). Use
\code{\link[=swadl_id_names]{swadl_id_names()}} to see available indicators.}

\item{measure}{Measure ID (e.g., \code{"nominal_wage"}). Use
\code{\link[=swadl_id_names]{swadl_id_names()}} or \code{\link[=swadl_indicator]{swadl_indicator()}} to see available measures.}

\item{date_interval}{Either \code{"year"} or \code{"month"}. Defaults to \code{"year"}.}

\item{geography}{A geography specification. Accepts state names (e.g.,
\code{"California"}), abbreviations (e.g., \code{"CA"}), region names (e.g.,
\code{"Midwest"}), division names (e.g., \code{"Pacific"}), or API IDs (e.g.,
\code{"state06"}). Defaults to \code{"national"}.}

\item{dimension}{Dimension specification. Can be:
\itemize{
\item \code{"overall"}: Returns aggregate data without demographic breakdown
\item A dimension ID (e.g., \code{"wage_percentile"}): Returns all values for that
dimension
\item A list with named and/or unnamed elements:
\itemize{
\item Named elements filter to specific values:
\code{list("wage_percentile" = "wage_p50")}
\item Unnamed elements return all values:
\code{list("wage_percentile")}
\item Multiple dimensions can be cross-tabulated, but only one dimension
can return all values; the others must specify values:
\code{list("gender" = "gender_male", "age_group")}
}
}}

\item{date}{Optional date filter. Can be:
\itemize{
\item \code{NULL} (default): All available dates
\item A single date (character or Date): Returns only that date
\item A vector of two dates: Returns dates in that range (inclusive)
}}
}
\value{
A tibble with columns:
\itemize{
\item \code{date}: Observation date
\item \code{value}: The observed value
\item \code{geography}: Geography ID
\item One column per dimension in the request, containing dimension value IDs
}
}
\description{
Retrieves time series data from the EPI State of Working America Data
Library API.
}
\examples{
\donttest{
# Median hourly wage over time
get_swadl(
  "hourly_wage_percentiles",
  "nominal_wage",
  dimension = list("wage_percentile" = "wage_p50")
)

# All wage percentiles
get_swadl(
  "hourly_wage_percentiles",
  "nominal_wage",
  dimension = "wage_percentile"
)

# Employment rate for males by age group
get_swadl(
  "labor_force_emp",
  "percent_emp",
  date_interval = "month",
  dimension = list("gender" = "gender_male", "age_group")
)

# Filter to specific date range
get_swadl(
  "hourly_wage_percentiles",
  "nominal_wage",
  dimension = "wage_percentile",
  date = c("2000-01-01", "2024-01-01")
)
}
}
\seealso{
\code{\link[=swadl_indicator]{swadl_indicator()}} for indicator details, \code{\link[=swadl_id_names]{swadl_id_names()}} to
list indicators, measures, and dimensions.
}
