% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/availability.R
\name{swadl_availability}
\alias{swadl_availability}
\title{Find available data across all indicators}
\usage{
swadl_availability(
  indicator = NULL,
  measure = NULL,
  date_interval = NULL,
  geo_level = NULL,
  dimensions = NULL,
  dimensions_match = c("exact", "all", "any")
)
}
\arguments{
\item{indicator}{Character vector of indicator IDs to filter to. If \code{NULL}
(the default), includes all indicators.}

\item{measure}{Character vector of measure IDs to filter to. If \code{NULL}
(the default), includes all measures.}

\item{date_interval}{Character vector of date intervals to filter to.
Valid values are \code{"year"}, \code{"quarter"}, and \code{"month"}. If \code{NULL}
(the default), includes all date intervals.}

\item{geo_level}{Character vector of geographic levels to filter to.
Valid values are \code{"national"}, \code{"state"}, and \code{"division"}. If \code{NULL}
(the default), includes all geographic levels.}

\item{dimensions}{Character vector of dimension IDs to match. How these
are matched depends on \code{dimensions_match}. If \code{NULL} (the default),
no dimension filtering is applied.}

\item{dimensions_match}{How to match the \code{dimensions} argument:
\describe{
\item{\code{"exact"}}{The dimensions column must exactly match the provided
dimensions (order-insensitive). For example, \code{c("gender", "race")}
matches \code{"gender × race"} but not \code{"age_group × gender × race"}.}
\item{\code{"all"}}{The dimensions column must contain ALL provided dimensions
(may contain more). For example, \code{c("gender", "race")} matches both
\code{"gender × race"} and \code{"age_group × gender × race"}.}
\item{\code{"any"}}{The dimensions column must contain ANY of the provided
dimensions. For example, \code{c("gender", "race")} matches \code{"gender"},
\code{"race"}, \code{"gender × race"}, and \code{"age_group × gender"}.}
}}
}
\value{
A tibble with columns:
\describe{
\item{indicator_id}{Indicator identifier}
\item{indicator_name}{Human-readable indicator name}
\item{date_interval}{\code{"year"}, \code{"quarter"}, or \code{"month"}}
\item{measure_id}{Measure identifier}
\item{geo_level}{\code{"national"}, \code{"state"}, or \code{"division"}}
\item{dimensions}{Dimension combination (e.g., \code{"gender × race"}) or
\code{"overall"} for aggregate data}
\item{date_start}{Start of available date range}
\item{date_end}{End of available date range}
}
}
\description{
Searches across all indicators to find which data is available matching
specified criteria. Useful for answering questions like "Which indicators
have state-level data by race?"
}
\examples{
\donttest{
# Find all indicators with state-level gender data
swadl_availability(geo_level = "state", dimensions = "gender",
  dimensions_match = "any")

# Find indicators with a specific measure
swadl_availability(measure = "percent_emp")

# Find all availability for a specific indicator
swadl_availability(indicator = "hourly_wage_percentiles")

# Find indicators with exact "gender × race" combinations at national level
swadl_availability(geo_level = "national",
  dimensions = c("gender", "race"), dimensions_match = "exact")
}
}
\seealso{
\code{\link[=swadl_indicator]{swadl_indicator()}} for detailed information about a single
indicator, \code{\link[=swadl_id_names]{swadl_id_names()}} to list all indicators.
}
